from distutils.core import setup 

setup(name = 'lda_topic',
      version = '1.0',
      author = 'willz_zhu', 
      #install_requires=["tornado"],
      packages  = ['lda_topic']   #py_modules
     )
